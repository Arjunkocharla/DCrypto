import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // or whereever your App.js file is located

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);